/**
 * Settings has no rail panel: its sub-navigation lives inside the main area (SettingsView),
 * so the sidebar shows nothing below the icon rails while the section is active.
 */
export function SidebarSettings() {
  return null
}
